'use client';

import React from 'react';

// Placeholder feature component
export function StudentModuleReading({ moduleId }: { moduleId: string }) {
  console.log('StudentModuleReading feature rendered for moduleId:', moduleId);
  return <div>Student Module Reading Placeholder for {moduleId}</div>;
} 