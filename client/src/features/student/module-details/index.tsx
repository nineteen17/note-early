'use client';

import React from 'react';

// Placeholder feature component
export function StudentModuleDetails({ moduleId }: { moduleId: string }) {
  console.log('StudentModuleDetails feature rendered for moduleId:', moduleId);
  return <div>Student Module Details Placeholder for {moduleId}</div>;
} 