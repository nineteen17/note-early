'use client';

import React from 'react';

// Placeholder feature component
export function AdminViewModuleProgress({ moduleId }: { moduleId: string }) {
  console.log('AdminViewModuleProgress feature rendered for moduleId:', moduleId);
  return <div>Admin View Module Progress Placeholder for {moduleId}</div>;
} 